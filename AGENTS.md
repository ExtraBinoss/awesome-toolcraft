# Project instructions

## Node and npm commands

This project runs in Linux. Use the local Node/npm toolchain directly from the
project directory for npm, npx, Node, Vite, TypeScript, tests, and builds.
